GophProfile has been deployed.

Server service:
  {{ include "gophprofile.fullname" . }}-server.{{ .Release.Namespace }}.svc.cluster.local

Worker metrics service:
  {{ include "gophprofile.fullname" . }}-worker-metrics.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.workerMetricsService.port }}

{{- if .Values.ingress.enabled }}
Ingress hosts:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- end }}

Health endpoint:
  /health

Metrics endpoint:
  /metrics
